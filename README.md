This todo app has database authentication using passport and an express session but as the project was taking time, v1 has not implemented cors or polling hence 
there are no collaborative lists, subtasks or messaging capabilities. Socket IO will be used and CORS is to be implemented for the build to work as now it uses a proxy in vite.
